Algoritmo "tabuada"

Var

    i,n, produto : inteiro

Inicio

   escreval("de qual numero queres a tabuada?")
   leia(n)
   
   para i de 1 ate 10 faca
        produto <- n * i
        escreval(n, " x ", i, " = ",produto)
   fimpara


Fimalgoritmo
